import { Matches } from './Matches';

// TODO: write tests
/*
- Test search bar
- test filter by company
- test all matches render
- test no matches message
*/
describe('Matches', () => {
    it('needs to get test written', () => {
        expect(0).toBe(0);
    });
});
